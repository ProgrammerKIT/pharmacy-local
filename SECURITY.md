# 安全範圍

這是個人自用、尚未經獨立安全審計的軟體。客戶資料在 App 內加密，在已配對 iPhone／Mac 的私人區網以 HTTPS 交換。程式沒有客戶上傳端點、外部 AI、CDN、遙測或錯誤報告。發布工具只接受明列的程式路徑；客戶檔案即使加密，也不在 GitHub 發布範圍。

## 對外連線與信任

Mac 更新器只從設定的公開 GitHub Release 發出 GET 下載，允許 HTTPS 的 github.com、release-assets.githubusercontent.com、objects.githubusercontent.com 轉址，驗證 Ed25519 簽章後才解包。請求不帶客戶內容、資料庫 ID、配對權杖或裝置名稱；GitHub 仍會得知網路 IP、請求時間與下載資源。這不是整台 Mac「完全不上網」。

發布私鑰與客戶資料庫金鑰是兩把不同的鑰匙。自動 CI 簽署方案需用戶明確同意把發布私鑰設定成該 GitHub 庫的 Actions Secret。私鑰不在 Git 程式碼內；但能修改可信 workflow／取得寫入與發布權限的人可能濫用它。請保護帳號、限制寫入者，保留加密離線私鑰備份。不要給不可信程式碼或 PR 執行帶私鑰的工作。

簽章只證明來自持有發布私鑰的一方，不能證明程式無漏洞或無惡意。CSP 與 Service Worker 的同源限制是瀏覽器防線，不是 macOS 作業系統沙箱。更新器、下載的 Node 服務在同一個 Mac 使用者權限下執行，沒有不可繞過的 OS 隔離；可信更新遭入侵仍可能危及本機檔案及瀏覽器解密內容。不能保證零外洩。

## 更新安全措施

- 純程式固定路徑白名單、禁止重複路徑／符號連結、限制包大小、逐檔雜湊、Ed25519 驗證。
- 精確資料格式與協定相容性；以發布序號與版號拒絕自動倒退。明確人工退版另行處理。
- 獨立版本目錄與狀態交易；新服務在未通過健康檢查前不接受業務請求。中斷交易下一次啟動回復原程式。
- 更新前保存額外密文檢查點。失敗不還原客戶資料，避免抹掉剛同步的紀錄；自動快照輪替不會刪這些升級檢查點。
- 在線分頁回報是否編輯／匯入中；安全確認後暫停新操作及服務寫入。兩階段 Service Worker 啟用避免強制丟失草稿。
- 沒有用戶設定的信任來源時不向外下載。手動暫停可保留本機使用與同步。

健康檢查驗證啟動、TLS、程式版號、必要檔案與密文外層格式。Mac 服務沒有客戶資料庫密碼，因此不能聲稱已核對解密後的客戶筆數、備註內容或所有功能。

## 本機仍需保護

免日常密碼是便利性取捨：IndexedDB 保存不可匯出的 CryptoKey；同源執行的程式仍能使用它解密。取得已解鎖裝置或瀏覽器的人可開啟資料。使用裝置鎖定、FileVault 與可信輸入法／擴充功能，設定好雲端備份排除，避免照片或匯出檔自動進 iCloud。

PWA 不能完整控制 iOS 備份、瀏覽器淘汰儲存、惡意程式、鍵盤／剪貼簿、螢幕擷取或其他 App。尚未儲存的草稿在切到背景時遮蔽但暫留記憶體，以免切 App 就丟失；系統強制關閉仍可能使草稿遺失。

資料庫密碼只在首次配對、復原或修復時用來衍生金鑰；正常瀏覽不輸入。它不同於 Mac 管理權杖、TLS 私鑰或發布私鑰，不可互換。忘記密碼且無可用副本沒有雲端找回服務。

所有已配對裝置都是可信寫入者；Mac 不解密，無法識別惡意密文的語意。撤銷配對不會刪除遺失手機已有資料，也不會自動輪替資料庫金鑰。備份不是同步，需另保留可還原的加密離線副本。
