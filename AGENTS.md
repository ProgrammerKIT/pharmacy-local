# 藥局關係筆記工作規則

收到使用者手動上傳的一批 CSV，先執行 `SOP1.md`，完成逐欄原文留存、跨來源門市候選與例外核對，再處理 App 匯入與關聯分析。

- 最新批次範圍以本次明確選取的檔案為準。歷史檔只比對，不混入新批次。
- 先回報跨批原檔是否相同。沒有裝置現況時，不聲稱已精確比對 App，也不要求使用者提供密碼。
- 原文不改寫；未提供的欄位不猜填；不可因同名而合併分店或人物。待核對不等於丟棄。每次確認提交的 CSV 必須先保存完整 Source Snapshot；略過／排除只影響衍生資料，不能使原始檔消失。
- 首次出現且未命中既有裁定的門市身分疑點，須在匯入或合併前交使用者裁定。既有裁定只在外部地點識別、允許名稱及已確認地址仍一致時沿用；新名稱、ID／地址變更、人工修改或同步衝突須重新核對。
- 客戶資料、原CSV及產出整理表放在私人資料工作區，不加入 Git、PR、issue、release或測試 fixture。
- 新版流程至少驗證重傳、來源版本衝突、App手動文字、不同分店、原始來源與加密同步；不以靜態檢查冒充裝置驗證。
- 本規則只適用此專案，不建立跨專案通用偏好或定時上傳任務。
