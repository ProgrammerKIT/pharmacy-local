# SOP1 — CSV 匯入前篩選、原文整理與門市整合

流程版本：1.0.0。適用於藥局關係筆記每次手動選取一批 CSV；不定時重匯，不掃描外部帳戶。這是資料進入 App 與關聯探索前的固定流程。

## 範圍與不變條件

- 保留所有原始位元、欄位、標點、換行、空白與來源。顯示層的排版處理另存；比對鍵的全半形或臺／台正規化不回寫原文。
- 本次檔案是本批；歷史檔案只用來比對。不以檔名括號編號或上傳時間判斷資料新舊。
- 門市整合是先建立可追溯的候選與分來源檢視，不是自動合併 App 已存在的客戶紀錄。不移走拜訪、附件或刪掉資料。
- 未確認的門市、來源版本、手動文字衝突先隔離。整批阻擋項均已處理或明確排除後才允許加密提交。
- 不用本批缺席判定刪店；不以匯入時間代替拜訪日期；不憑姓名、比喻或關鍵字推定人物身分與關係。

## 固定步驟

1. **接收與存證**：建立來源檔 SHA256、清單名、欄名、實體行與原始列指紋。每次只處理明確選取的檔案；UTF-8／BOM或手動指定編碼，解析引號內換行與逗號。解析失敗或超過限額時整批不寫入。
2. **欄位品質檢查**：核對必要名稱、欄位映射、重複欄名、未對應非空文字、字數與網址。純清單標籤行保留原檔但不建門市；缺名稱且有座標／文字不是標籤行，需補正或明確排除。
3. **原文整理**：逐欄核算文字是否有去處；筆記、留言、標籤與其他文字不得靜默漏掉。額外非空欄位須確認保存為來源補充；需做主備註時重新對應欄位。保留否定、問題、不確定語句與日期上下文，不在前處理改寫商業或醫療意義。
4. **門市辨識**：使用可靠的 Google 地點 ID；其次使用有來源的名稱＋地址。短網址、一般搜尋、座標、只有名稱均需核對。相同 ID 的不同名稱／地址仍可能是改名或錯連，不能自動裁定。
5. **整店來源對齊**：將各清單列連到確認的門市，保留清單／原名／行號。清單不同不代表時間不同；不把清單標籤當成每一間店的已確認事實。
6. **與 App 現況比對**：在裝置上用當下資料庫生成預覽。離線 audit 只能比對提供的檔案；沒有讀取裝置時，結果必須註明尚未完成 App 比對。
7. **補欄位與例外處理**：有來源且明確勾選才補空白欄位，不覆蓋非空值。必填錯誤、未知文字處置、手動文字差異、門市錯連或來源同步衝突須先處理。可明確排除本批某列；排除不等於從歷史刪除。
8. **一次提交與後段入口**：預覽後資料有變更就重做；全部阻擋項解除且使用者確認後，產生完整新資料包再加密儲存。新 CSV 不自動生成主題／人物。關聯探索對符合條件的相同來源全文整合顯示，但原始來源紀錄與操作仍各自存在。
9. **驗收與追溯**：檢查原文完整、重傳不加版本、手動資料保留、空白來源保留舊文、不同分店不誤併、後續同步保留版本衝突。程式改動須通過既有測試及 SOP1 測試。

## 重複／更新決策表

| 情況 | 固定處理 |
|---|---|
| 同一清單、同一原始列，且目前 App 來源文字與日期狀態一致 | 略過，不新增門市或備註操作；保留已有來源 |
| 同店、不同清單、全文及日期／人工連結／附件一致 | 保留不同來源串流；關聯探索只整合顯示相同文字。這不是認定同一次拜訪 |
| 同店、不同清單、不同全文或僅部分句子重疊 | 保留所有原文與來源，不剪句、不拼出來源未曾寫過的句子 |
| 同店、同清單，新文字或明確拜訪日期變更 | 建立來源新版本；歷史保留，不當成另一場拜訪 |
| App 手動文字與 Google 新版不同 | 先核對；保留 App 文字，另存 Google 新文字並標示待確認 |
| 同店同清單在同批有兩份不同版本 | 整批阻擋；須決定保留哪份或改正來源，不以處理先後決定新舊 |
| 最新來源備註空白 | 保留舊文，標示來源缺失；本批未出現的店保持原樣 |
| 同名不同地點 ID／相同 ID 名稱矛盾 | 候選提示；明確指定門市或新增獨立門市後才提交 |

## 可重跑的前處理

`scripts/sop1-audit.mjs`只讀明確給定的本機 CSV，以相同規則輸出原始列、來源控制、地點候選、文字、例外與跨批雜湊比對。命令列提供的資料路徑與輸出應放在私人資料目錄，不能在程式儲存庫內。

```sh
node scripts/sop1-audit.mjs --output /private/result.json \
  --previous /private/previous.csv /private/current.csv
```

多份來源可重複 `--previous` 與輸入路徑。audit 不登入 App、不解密使用者資料、不寫入門市，也不把輸出當成已審核的匯入檔。工作表可供核對；實際提交以裝置內預覽為準。

## 已實作與限制

- `public/csv.js`：SOP1 報告與提交阻擋、來源補充確認、不同來源版本隔離、日期修正、來源版本保留。
- `public/csv-ui.js`：每批預覽顯示 SOP1 狀態、未完成事項與確認欄位。
- `public/relations.js`：唯讀證據分組；不改動資料庫。
- `scripts/sop1-audit.mjs`：離線文件核對；沒有 App 現況就明示缺口。8字以上相同整行／全文包含是文字線索，不是語意等價偵測器。
- `test/sop1.test.mjs`及既有測試使用虛構資料。GitHub 只保留程式、SOP與虛構測試；不放客戶CSV、整理結果、密碼、附件或加密資料庫。
- 每台裝置需要取得含本改動的正式發佈後，才會執行這版 SOP1。文件或 PR 的存在不代表已更新到裝置。
